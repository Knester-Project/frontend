const New = () => {
    return ( 
        <main>
            Create New Chat Form, should have an input field where users with the entered username are shown and messages can be sent
        </main>
     );
}
 
export default New;